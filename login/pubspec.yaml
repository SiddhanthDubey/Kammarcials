name: login
description: "A new Flutter project."
publish_to: 'none'  # Replace 'none' with your desired publishing destination
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  http: ^0.13.3
  
  google_sign_in: ^5.2.1
  cloud_firestore: ^3.1.5
  provider: ^6.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
